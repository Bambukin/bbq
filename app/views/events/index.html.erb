<h1><%= t('events.all') %></h1>

<% if policy(Event).create? %>
  <div class="buffer-both">
    <%= link_to t('link.create_event'), new_event_path, class: 'btn btn-success' %>
  </div>
<% end %>

<div class="row buffer-bottom">
  <%= render partial: 'events/event', collection: @events %>
</div>
